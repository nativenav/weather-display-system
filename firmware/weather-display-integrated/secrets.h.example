/**
 * WiFi Credentials Configuration Template
 * Weather Display Integrated - XIAO ESP32C3 + 7.5" ePaper
 * 
 * 📋 SETUP INSTRUCTIONS:
 * 1. Copy this file to "secrets.h" 
 * 2. Update the WiFi credentials with your actual networks
 * 3. Upload to your ESP32C3
 * 
 * ⚠️  SECURITY: Never commit the actual secrets.h file to git!
 */

#pragma once

// WiFi Network Configuration
struct WiFiNetwork {
  const char* ssid;
  const char* password;
};

// 🔧 REPLACE WITH YOUR ACTUAL WIFI CREDENTIALS:
const WiFiNetwork WIFI_NETWORKS[] = {
  {"YourHomeNetwork", "your_wifi_password_here"},
  {"YourMobileHotspot", "hotspot_password"},
  {"OfficeGuest", "office_guest_pass"},
  // Add more networks as needed
};

const int NUM_WIFI_NETWORKS = sizeof(WIFI_NETWORKS) / sizeof(WIFI_NETWORKS[0]);

// Backend Configuration (✅ Production backend)
#define BACKEND_URL "https://weather-backend.nativenav.workers.dev"

// Device Settings
#define DEFAULT_STATION "prarion"     // See setup instructions for options
#define DEVICE_NAME "Weather Display"

// Debug Settings
#define ENABLE_SERIAL_OUTPUT 1        // Serial debugging
#define SERIAL_BAUD_RATE 115200

// Power Management
#define ENABLE_DEEP_SLEEP 0           // Battery saving mode
#define SLEEP_MINUTES 5

/*
 * Available Weather Stations:
 * 
 * CHAMONIX VALLEY (French Alps):
 * - "prarion" - Prarion (Les Houches), 1,865m elevation
 * - "tetedebalme" - Tête de Balme, 2,204m elevation  
 * - "planpraz" - Planpraz, 1,958m elevation
 * 
 * SOLENT (UK Marine):
 * - "brambles" - Brambles Bank, Solent sailing area
 * - "seaview" - Seaview, Isle of Wight
 * - "lymington" - Lymington, Hampshire
 * 
 * After first connection, visit the web interface to:
 * - Rename your device
 * - Change station assignment  
 * - Use identify feature to flash the display
 * - Monitor device status
 * 
 * Web Interface: https://0b4669b0.weather-management.pages.dev
 */
